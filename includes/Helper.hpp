#ifndef HELPER_HPP
#define HELPER_HPP

#include "utils.h"

class Helper
{
private:
    /* data */
public:
	int				findLen(Client &client);
	int				fromHexa(const char *nb);
    void		    fillBody(Client &client)


    Helper(/* args */);
    ~Helper();
};

#endif